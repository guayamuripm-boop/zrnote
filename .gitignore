node_modules/
.next/
dist/
*.log
.DS_Store
.turbo/
tsconfig.tsbuildinfo
.vercel
nul

# Secretos: nunca versionar.
.env*
!.env.example

# Clave privada de firma de la extensión de Chrome.
# Quien la tenga puede publicar una actualización firmada como si fuera tuya.
*.pem
extension.crx


# Respaldos locales (snapshots de codigo). No se versionan ni se suben.
.backups/

# Snapshots y worktrees locales de sesiones de trabajo.
.backup/
.claude/worktrees/
.claude/settings.local.json

# Estado local del CLI de Supabase (contiene el ref del proyecto enlazado).
supabase/.temp/
